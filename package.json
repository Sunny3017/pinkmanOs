{
  "name": "pinkman-os",
  "version": "1.0.0",
  "description": "Cyberpunk Phone Lookup Dashboard",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "production": "npm run build:frontend && cd backend && cross-env NODE_ENV=production npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
